# AIRS Demo Environment Variables (YAML for Cloud Run — served from staging infra)
# Usage: gcloud run deploy --env-vars-file gcp/env.staging.yaml

ENV: "demo"
APP_NAME: "ResilAI"
DEBUG: "false"
GCP_PROJECT_ID: "gen-lang-client-0384513977"

# Prefer managed DB in staging; SQLite fallback shown for bootstrapping only.
DATABASE_URL: "sqlite:///./airs_staging.db"

# Set to your staging frontend URL(s)
CORS_ALLOW_ORIGINS: "https://airs-staging-0384513977.web.app,https://airs-staging-0384513977.firebaseapp.com,https://gen-lang-client-0384513977.web.app,https://gen-lang-client-0384513977.firebaseapp.com"

AUTH_REQUIRED: "true"
DEMO_MODE: "false"
AIRS_USE_LLM: "true"
LLM_PROVIDER: "gemini"
LLM_MODEL: "gemini-3-flash-preview"
