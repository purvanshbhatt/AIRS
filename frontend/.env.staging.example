# Staging build variables (Vite mode: staging)
# Copy this file to: frontend/.env.staging
#
# Build:
#   npm run build:staging
#
# Deploy hosting to your STAGING Firebase project:
#   firebase deploy --only hosting --project <your-staging-project-id>

# Staging API (Cloud Run) - set this after you deploy the staging Cloud Run service
VITE_API_BASE_URL=https://airs-api-staging-<project-number>.us-central1.run.app

# App name (optional)
VITE_APP_NAME="AIRS (Staging)"

# Staging Firebase project (Auth + Hosting)
VITE_FIREBASE_API_KEY=your-staging-api-key
VITE_FIREBASE_AUTH_DOMAIN=your-staging-project-id.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-staging-project-id
