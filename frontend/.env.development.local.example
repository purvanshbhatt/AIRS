# Local development overrides (Vite mode: development)
# Copy this file to: frontend/.env.development.local
#
# Goals:
# - Frontend calls local FastAPI: http://localhost:8000
# - Auth is safe: use Firebase Auth Emulator (recommended) OR a separate staging Firebase project
#
# NOTE: This file is safe to commit (no secrets).

# Local API (FastAPI)
VITE_API_BASE_URL=http://localhost:8000

# App name (optional)
VITE_APP_NAME="AIRS (Local)"

# -----------------------------------------------------------------------------
# Option A (Recommended): Firebase Auth Emulator (no real users created)
# -----------------------------------------------------------------------------
# 1) Install Firebase CLI: https://firebase.google.com/docs/cli
# 2) From repo root, start emulator:
#      firebase emulators:start --only auth
# 3) Uncomment the block below.
#
# VITE_FIREBASE_API_KEY=fake-api-key
# VITE_FIREBASE_AUTH_DOMAIN=localhost
# VITE_FIREBASE_PROJECT_ID=demo-airs
# VITE_FIREBASE_AUTH_EMULATOR=http://localhost:9099

# -----------------------------------------------------------------------------
# Option B: Staging Firebase project (real auth, but isolated from production)
# -----------------------------------------------------------------------------
# Create a separate Firebase project for staging and copy values from:
# Firebase Console -> Project Settings -> General -> Your apps
#
# VITE_FIREBASE_API_KEY=your-staging-api-key
# VITE_FIREBASE_AUTH_DOMAIN=your-staging-project-id.firebaseapp.com
# VITE_FIREBASE_PROJECT_ID=your-staging-project-id
